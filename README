Compile the wrapper and the demos:
make all

Just compile the wrapper:
make lib

Use it:
LD_PRELOAD=./glQuadBufferEmu.so YourQuadBufferedApplication

Example of use: 
LD_PRELOAD=./glQuadBufferEmu.so glxgears -stereo

TODO:
- parse config file
- switch from one mode to another
- sync SwapBuffer with vblank (frame sequential)
- new interlaced mode (vertical; horizontal; checkerboard ?)
- Dubois algorithm in anaglyph mode

Mode available:
- Monoscopic (Left or Right),
- Anaglyph (any color),
- Side-by-side (Left/Right; Top/bottom; Frame-packing).
- Interlaced (is it working ?)
- Frame sequential (software page-flipping)
